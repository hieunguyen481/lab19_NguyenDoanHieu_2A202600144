# Scripts

PowerShell helper hiện nằm ở project root: `run_day19.ps1`, để chạy nhanh từ folder `C401_D3_Day19`:

```powershell
.\run_day19.ps1 offline
.\run_day19.ps1 openai-generate
.\run_day19.ps1 openai-extract
.\run_day19.ps1 neo4j-export
```

Folder này để dành cho các script phụ nếu cần mở rộng sau.
